"""It is called by IGFactory (SinglePlayerGFactory.py) to make the game engine and return it to the caller
It accepts the input parameters and makes the game engine

gameengine contains players, turning strategy, board (and cells inside it), winier checking,
move validation, a list of subcribers (UIs, voice, etc.)
"""